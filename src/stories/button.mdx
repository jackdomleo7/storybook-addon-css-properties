import { Meta, Title, Canvas, Story } from '@storybook/addon-docs/blocks';
import * as buttonStories from './Button.stories';

<Meta of={buttonStories} />

<Title>Button</Title>

Example with `Canvas`:

```tsx
<Canvas of={buttonStories.Custom} />
```

<Canvas of={buttonStories.Custom} />

Example with `Story`:

```tsx
<Story of={buttonStories.Custom} />
```

<Story of={buttonStories.Custom} />